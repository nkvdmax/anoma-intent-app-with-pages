{
  "name": "anoma-intent-app-with-pages",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "postinstall": "node -e \"try{const fs=require('fs'),p='node_modules/.bin';if(fs.existsSync(p)){for(const f of fs.readdirSync(p)){try{fs.chmodSync(p+'/'+f,0o755)}catch{}}}}catch{}\""
  },
  "engines": {
    "node": "22.x"
  }
}